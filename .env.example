# ==========================================
# ERP Backend Configuration
# ==========================================

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=Erp
DB_USER=postgres
DB_PASSWORD=postgresql123

# API Server
API_PORT=5000
API_BASE_URL=https://gumming-collapse-subside.ngrok-free.dev

# JWT Secret (change in production!)
JWT_SECRET=default_erp_jwt_secret_key

# SMTP (optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=you@gmail.com
# SMTP_PASS=app-password
# SMTP_SENDER_NAME=ERP System
# SMTP_ENABLE_TLS=true
